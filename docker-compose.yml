services:
  upload-service:
    image: jiasdocker/upload-service:latest
    ports:
      - "5000:5000"
    volumes:
      - ./data:/data
    environment:
      - DATA_DIR=/data
    restart: unless-stopped
    container_name: upload-service